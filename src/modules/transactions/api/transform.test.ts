import { transformGetTransactions } from './transform';

describe('transform transaction data', () => {
  it('should return an array of object and validate the contents', () => {
    transformGetTransactions;
  });
});
